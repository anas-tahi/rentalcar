# API base URL — points to the API Gateway
REACT_APP_API_URL=http://localhost:5000

# PayPal client ID (browser-safe — public key)
REACT_APP_PAYPAL_CLIENT_ID=your_paypal_sandbox_client_id

# Stripe publishable key (browser-safe — public key)
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_placeholder
